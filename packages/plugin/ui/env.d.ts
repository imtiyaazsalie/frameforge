/// <reference types="vite/client" />

// Injected at build time by Vite `define` (see vite.config.ts) — the product
// version, sourced from frameforge-mcp's package.json.
declare const __APP_VERSION__: string;
