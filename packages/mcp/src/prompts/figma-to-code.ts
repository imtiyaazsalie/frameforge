import type { GetPromptResult, Prompt } from '@modelcontextprotocol/sdk/types.js';
import { z } from 'zod';

// The cross-client twin of the design-to-code Claude Code skill: a distilled, guided workflow any MCP
// client (Cursor / Windsurf / Claude Desktop) can surface as a slash command. The deep version lives
// in skills/design-to-code/SKILL.md; this is intentionally the short form — it names the
// three grounded tools, their order, and the reuse/token rules, which is what a client without the
// skill needs. Kept short so it tracks the skill's intent without re-deriving its detail.

export const FIGMA_TO_CODE_PROMPT_NAME = 'figma_to_code';

/** Build the guided-workflow text, pointing the three tools at a node id or the current selection. */
const promptText = (nodeId: string | undefined): string => {
  const target = nodeId === undefined ? 'the current Figma selection' : `Figma node ${nodeId}`;
  const arg =
    nodeId === undefined ? '(they default to the current selection)' : `with nodeId "${nodeId}"`;
  return `Generate code for ${target} that matches this project — reuse what already exists, build only what is missing. Do not guess layout from a screenshot; ground every decision in these tools, called ${arg}:

1. get_design_context (detail: full, dedupeComponents: true) — the layout tree with tokens resolved to names and each instance's componentProperties. A resolved variable may also carry codeSyntax ({ WEB: "--color-primary", … }) — the designer's declared code-side token name, stronger than any name derived from the Figma name; use the stack's platform key verbatim when it matches a token that exists in the project, but when it matches nothing (stale after a rename) trust the project's actual token found by value/name. Do not depth-limit a subtree you intend to build from: the first instance of a repeated component keeps its full structure while later ones show "deduped". A deduped instance still carries textOverrides (the visible text it actually renders, { name, characters } per TEXT) — fill each repeated element's content from that, since the structure is identical but the text differs per instance; don't copy the first instance's text or re-fetch the un-deduped tree just to get distinct titles / labels / rows. A deduped instance also carries propertyOverrides — the visual fields it renders differently from the main component ({ name, fills/strokes/effects/opacity/cornerRadius/visible } per overridden node): apply these per instance the same way as textOverrides, or every instance collapses to the main component's styling and a per-instance accent (the highlighted plan's colour, a hidden badge) is silently lost. Per node, carry every visual property the tree exposes, not just layout: fills, strokes, cornerRadius, opacity, and especially effects (a node's effects / styleIds.effect = a drop/inner shadow or blur — translate it to the stack's shadow utility). Effects are the easiest fidelity to drop because they come from a shared effect style and read as a single field; a card with no shadow when the design has one is the classic grounding miss. A blur effect's type decides the CSS: LAYER_BLUR blurs the element itself (filter: blur), but BACKGROUND_BLUR blurs what shows through from behind (backdrop-filter: blur — the frosted-glass / glassmorphism pattern, typically paired with a semi-transparent fill); emitting BACKGROUND_BLUR as filter, or dropping it, loses the glass look entirely. When strokeWeight is "mixed" the node also carries strokeWeights { top, right, bottom, left }: emit only the non-zero sides (border-t / border-b / …), never a uniform border — collapsing a per-side stroke into a full border turns a table row divider or underline input into a full grid. A stroke also carries strokeAlign (INSIDE / OUTSIDE / CENTER): INSIDE is a plain inset border, but OUTSIDE draws outside the box — emit it as an outline or box-shadow (0 0 0 Npx colour), never a plain CSS border, so it doesn't grow the box or shift its position (selection rings and focus outlines are OUTSIDE); CENTER straddles the edge (half the weight each side). When cornerRadius is "mixed" the node carries cornerRadii { topLeft, topRight, bottomRight, bottomLeft }: emit only the rounded corners (rounded-t / rounded-tl / …), never a uniform radius — a card rounded on one edge, a tab or a chat bubble round only some corners, and collapsing to a single radius squares them off or rounds the wrong side. A node may also carry blendMode (MULTIPLY / SCREEN / OVERLAY / …): map it to CSS mix-blend-mode (on the element) or background-blend-mode (a fill over an image) — an overlay swatch blended onto a photo reads as the wrong flat colour if you drop it. A node with isMask: true is a mask (it clips its later siblings to its own shape, maskType ALPHA/LUMINANCE/GEOMETRY) — don't render it as an ordinary image/shape; realise it as the container's overflow-hidden + border-radius, a clip-path, or an SVG mask on the masked siblings, and skip emitting the mask layer itself as content. A GRADIENT_LINEAR/RADIAL/ANGULAR/DIAMOND fill carries gradientStops ({ position, color hex }) + gradientTransform (the 2×3 axis matrix): emit a real CSS gradient (linear-gradient/radial-gradient) from the stops with the direction derived from the transform — don't flatten a gradient to a solid colour (same miss class as a dropped shadow). An IMAGE/VIDEO fill carries scaleMode (the object-fit equivalent: FILL→cover, FIT→contain, CROP→cover+position, TILE→repeat) — apply it to the exported image so it isn't stretched or letterboxed. Each auto-layout frame carries a layout object with the exact spacing — read it, never reverse-engineer padding/gap/justify from child x/y/w/h (that's the same eyeballing error as guessing a colour off the screenshot): mode (HORIZONTAL/VERTICAL → flex-row/flex-col, GRID → display:grid), padding (paddingTop/Right/Bottom/Left → p-*), and for H/V itemSpacing (→ gap) + primaryAxisAlignItems/counterAxisAlignItems (→ justify-*/items-*, SPACE_BETWEEN → justify-between). A H/V frame may also carry itemReverseZIndex: true (later siblings paint UNDER earlier ones — the overlapping-avatars / fanned-cards stack, usually with a negative itemSpacing; CSS paints later DOM nodes on top, so reverse the z-index or the DOM + flex-direction, or the stack overlaps the wrong way) and strokesIncludedInLayout: true (borders take up layout space, which Figma excludes by default — fold the stroke weight into the gap/padding math, the box-sizing: border-box case). A GRID frame instead carries gridRowCount/gridColumnCount (→ grid-template-rows/columns: repeat(N, 1fr)), gridRowGap/gridColumnGap (→ gap), and optional gridRowSizes/gridColumnSizes tracks (FIXED→px, FLEX→fr) — emit a real CSS grid, don't flatten it to stacked flex; a grid child carries gridChild only when it's pinned or spanning (rowAnchorIndex/columnAnchorIndex → grid-row/grid-column anchor+1, rowSpan/columnSpan → span N, horizontalAlign/verticalAlign → justify-self/align-self) — a child with no gridChild is auto-flowed, so let the grid place it and don't emit an explicit grid-row/grid-column. A node's own layoutSizingHorizontal/Vertical (FILL→flex-1/stretch, HUG→fit-content, FIXED→explicit size) + layoutGrow + layoutAlign decide how it fills its auto-layout parent. A node may also carry targetAspectRatio ({ x, y }) — a locked width:height ratio (a 16:9 media box, a square avatar, a hero that must keep its shape across breakpoints): emit aspect-[x/y] (CSS aspect-ratio: x / y) and leave one dimension fluid instead of freezing both w and h to the measured pixels. A scrolling frame may carry numberOfFixedChildren — that many LEADING children stay pinned while the rest scroll (a sticky header / toolbar → position: sticky inside the scroll container), not ordinary flow children. A node may carry annotations — the designer's Dev Mode notes written FOR you ("use the brand token here", "hover only", "this is the live region"): authoritative ground truth that outranks anything you'd infer from geometry or the screenshot (properties names the fields a note is about, e.g. ["fills"]), so reflect the intent (the right token / state / ARIA) in the code and surface anything you can't express as a TODO rather than dropping it. If a whole page is too big to ground in one call (a dense ~330-node tree can exceed the context window / token cap), don't retry the same oversized call and don't depth-cap the whole page (a shallow tree drops the structure inside each section → empty cards): scope horizontally — get the top-level section node ids first (detail: minimal, or read the page's direct children), then get_design_context each section by its nodeId at full detail and build it before moving on. The tool enforces this on the worst cases: an over-budget call returns a sectionPlan ({ sections: [{ nodeId, name, nodes }] }) instead of the tree — ground each listed section by its nodeId, exactly as above. Ground EVERY section this way — never eyeball values (font-size, colour, padding, radius) off the screenshot for "the easy ones"; that produces a cascade of systematic errors (headings all too small, wrong accent colour, off paddings, a missing border) even though get_design_context had every exact value. The screenshot is visual intent only; if you're about to type a number you didn't read from grounding, re-get_design_context that node.

2. component_map — every Figma component grouped to a local code component with a status.
   - high / medium: import and reuse candidate.filePath; do not regenerate it.
   - candidate.ambiguousWith (a list of { name, filePath }): the name matched two+ code components nearly equally and the join could not confidently pick — a verify-me pick, not a confident reuse. Check which of the winner + these runner-ups is the right component for THIS context before importing (a wrong reuse is a silent visual bug), then record the confirmed one in docs/figma-component-map.md so next run is certain.
   - Wire each entry's instances[].props (resolved variant / boolean / text values) onto the reused component — one element per instance, each with its own props.
   - candidate.unmatchedProps are props the design needs but the component lacks → surface them as component-extension TODOs, never fake them with ad-hoc markup.
   - unmapped: build it new in the project's style. For a repeated unmapped component (instanceCount > 1, e.g. a table row), build from its first instance's subtree; drill instances[0].nodeId if it was scoped away.
   - To generate or extend a component's own definition (its prop types, not just one render), get_component_api on the component/instance returns its full property API — every VARIANT option and each BOOLEAN/TEXT/INSTANCE_SWAP prop with its default — so you emit the complete prop space, not just the props the instances you happened to see use.
   - Record a reuse the fuzzy join was unsure of but you VERIFIED — a low/medium candidate you confirmed, or an unmapped component you built — as a row in docs/figma-component-map.md (FigmaName | code/path). component_map reads it back next run as source: "map-file" (confidence 1), so the reuse is certain instead of re-guessed. A recorded row OVERRIDES the join on every future run and fails silently if wrong (a mis-map in every future generation until a human notices — worse than no record), so record only after step 6's render-verify confirmed it, never a guess; skip the already-high matches (noise); fix any staleOverrides it reports (a recorded target that moved / was deleted → it already degraded to the fuzzy result).

3. token_map — every Figma variable joined to a project token; a document's shared paint styles (single solid color styles — the token mechanism of pre-variables files) join the same way, marked source: "style". Reference candidate.ref (e.g. bg-primary-500 or var(--color-primary-500)), never the raw hex/px. On a document with few or no variables (most real-world files), get_design_context's own projectTokens map is the fallback: any raw color in the payload that exactly equals a project token's value is annotated there ({ "#6266F0": { ref, name, matchedBy: ["value"] } }) — before hardcoding a hex, look it up and emit the ref when it fits the context semantically. matchedBy: ["value"] marks the entry as name-blind value-equality evidence (a hypothesis to verify, not a resolved binding): an entry with candidates lists same-value tokens to choose between by meaning, a semantically wrong token is worse than the raw value (keep the raw value and note the gap instead), and a bound Figma variable always outranks a raw-value match. A candidate carrying ambiguousWith (several project tokens share that exact color value and the name could not split them) is a capped, verify-me pick — choose the semantically right sibling for the context (or keep the value and flag the gap), never treat it as a confirmed reuse. A 'framework-builtin' status (Tailwind built-in scale, e.g. spacing/4, line-height/7, weight/Bold) carries builtin.{scale,step}: for spacing compose the step with the bound property (p-4 / gap-4 / m-4), for line-height use leading-{step} (leading-7), for font-weight use font-{step} (font-bold) — use the utility, not an arbitrary p-[16px], and don't report it as a gap. A mapping with figmaModes ({ Light: "#FFFFFF", Dark: "#0A0A0A", … } — present when the variable's collection has multiple modes and its value differs per mode; the result's themedCollections lists the file's theme axes) is theme-dependent, and figmaValue is only the default mode — never emit that single literal as the token's one value (a dark-mode design shipped light-only is the token-level equivalent of a dropped shadow). Mapped: emit candidate.ref and confirm the project token itself switches per theme (redefined under .dark / [data-theme] / prefers-color-scheme, or a Tailwind dark: story); if the project defines only one value, wire the other mode's value into that mechanism or surface it as a theme gap. Unmapped: the default mode is the base value and each other mode rides the project's dark-mode mechanism (dark: variant / a prefers-color-scheme block). A file can also encode themes without native modes — paired collections or name groups (Color/Light/* + Color/Dark/*, a plan-limited workaround): when variables pair up that way, treat the pair as one theme-dependent token the same way. Only a genuinely 'unmapped' variable is a gap — use the value but call it out, don't hardcode it silently. Record a token resolution the join was unsure of but you VERIFIED — an ambiguousWith / matchedBy ['value'] / unmapped one you resolved by meaning — as a row in docs/figma-token-map.md (FigmaName | ref, where ref is what you'd emit: a utility bg-primary-500, a var(--color-primary-500), or the bare token name). token_map reads it back next run as matchedBy ['map-file'] (confidence 1), so an otherwise-ambiguous colour resolves for free. A recorded row OVERRIDES the join every future run and fails silently if wrong, so record only a mapping you verified (never a guess), and fix any staleOverrides it reports (a ref that no longer resolves).

4. Export the assets grounding can't encode — logos, photos, and icons have no pixels in the layout tree and otherwise render as grey blocks, often half the visible surface. A node with an IMAGE fill (a photo / product shot) → save_image_fills: it writes the ORIGINAL uploaded bytes (no clip, crop, scale, mask, or effects baked in), dedupes a reused asset by imageHash, and reports each fill's scaleMode + intrinsic width/height — reproduce the display in CSS (FILL→object-fit: cover, FIT→contain) instead of shipping a pre-cropped render that re-crops wrong the moment the container resizes; a clipped or off-canvas node still yields its full asset, and on path: null (or images: []) fall back to a screenshot. Reach for get_screenshot / save_screenshots PNG (scale 2) only when you specifically need the COMPOSITED look — a CROP region, a mask, a gradient overlay you can't reproduce in CSS, or Figma image adjustments (exposure / contrast / tint tuned on the fill itself — the original bytes don't carry them, so a colour-graded photo shipped from the original looks wrong; grounding flags this case as filtersApplied: true on the IMAGE fill, so export the composited render instead of trusting save_image_fills). A VECTOR / boolean-op, or an icon instance (e.g. mainComponent.name under Icons/…), exports as SVG via get_screenshot into the project's asset dir; import the real file. Logos and brand marks are always exported, never typed by hand. For icons, run icon_map FIRST and export only as the fallback: a high/medium match reuses the project's curated .svg (filePath + a colorContract) instead of re-exporting a duplicate — compose the import yourself from the filePath + profile.svg.importHint (loader form) and the project's own alias/relative convention (icon_map does not hand you a ready specifier; the alias/relative path is project-specific, so mirror how existing files import from the asset dir); an unmapped icon with a non-empty iconLibraries (lucide / heroicons / iconify) imports from that library; only with neither a file match nor a library do you get_screenshot SVG a fresh file. Color a single-color icon at the usage site, never bake it in (icon_map's colorContract says which path): currentColor → recolor with text-{token} (or color:), never fill-*, drive the token off the icon's Figma fill via token_map, keep the file as currentColor — but this only works inlined (component mode / library / inline svg), currentColor can't reach an <img> (url mode); fixed / multi-color → render as-is, don't recolor. For svg icons, import/use them per profile.svg (returned on component_map / token_map / icon_map): mode 'component' (a loader like svgr / vite-svg-loader is set up — profile.svg.importHint gives the exact form, which differs: ?react vs ?component vs { ReactComponent }) → import once and render <Icon/>, reusing the same import across every occurrence (dedupe: one file, one import, many uses); mode 'url' (no loader) → import the svg as a URL and use <img src> (or inline the svg when you need currentColor / CSS control). Never emit <Icon/> when mode is 'url' — that import won't run. If an export comes back empty:true (the node rendered nothing — hidden / fully clipped / off-canvas, e.g. a marquee's off-screen edge logos), don't ship the blank file: if grounding shows that instance has art, re-export its mainComponent; if the node is genuinely empty, skip it.

5. Responsive by default — output RWD, never hardcode the artboard width (a root w-[1920px] scrolls on every smaller screen; the root is w-full, sections fluid with content centered in a max-w). Ground the breakpoints, don't guess them: search_nodes (type FRAME, scoped to the file/page) lists sibling frames with their widths — match the desktop frame to its narrower counterparts by three signals: width buckets (~1920/1440 desktop, ~768 tablet, ~375 mobile), name normalization (strip device prefixes — W_/Ｍ_, Desktop/Mobile, PC/SP), and content similarity (same section order + matching textOverrides text, which works even when names don't follow a convention); then get_design_context each matched frame. Diff the layouts into one mobile-first base + lg:/xl: variants: most differences are reflow handled by a single markup with utilities (flex-col lg:flex-row, grid-cols-1 xl:grid-cols-3, hidden xl:block, alignment swaps); only a structure swap needs twin xl:hidden / hidden xl:block markup — when the content semantics differ (a CTA labelled 登入 on mobile vs 聯絡我們 on desktop) or the layout systems are incompatible (a hamburger vs a full nav, an absolute-positioned hero vs a flow stack). A fixed-width desktop layout's breakpoint must be ≥ its content width (gate a 1180px row at xl:/1280, not lg:/1024, or it overflows at in-between sizes). If no other-breakpoint frame exists, still output best-effort RWD (fluid container + sensible reflow) and note the responsive behaviour is inferred, not grounded.

6. Verify visually (close the loop) — don't ship code you never rendered. Render it with the project's own toolchain (e.g. Vite build + preview, Next build + start, or the existing dev server), mount what you built (a page route, or a throwaway harness for a single component) with tokens/CSS/exported assets wired so it isn't half-styled, then screenshot at the design's real viewport and diff against the Figma node (get_screenshot on the same node). Use Chrome headless via CDP Emulation.setDeviceMetricsOverride for an exact width — do not trust --window-size for exact-width/mobile shots (it renders wider than asked and crops a centered mx-auto root, faking a right-edge overflow); --headless=new --screenshot --window-size=1440,1500 is fine for wide desktop. For each real discrepancy, trace it to its source (re-get_design_context that node, don't re-guess from the screenshot) and fix the code, then re-render. Check both desktop and mobile widths and confirm zero horizontal overflow on mobile. There is no generic verify tool and shouldn't be — the render entry (dev server vs build+preview, port, route, how a component mounts, its props/runtime context) is project-specific, so you drive it with your shell + browser.

Emit code in the detected stack (the profile is returned on component_map / token_map). Rules: ground every section (every px size, colour, font-size, radius, spacing comes from get_design_context, for every section not just the first few — never eyeball the screenshot to save effort), reuse beats regenerate, reference tokens not literals, export visual assets rather than faking them with grey boxes or hand-typed wordmarks, output responsive code with a w-full root (never the artboard's fixed width), render and verify against the Figma node before calling it done, and mirror the project's existing import style, file layout, and naming. For an edge-to-edge (full-bleed) page, the body margin must be zeroed (a missing reset shows as a full-page white gutter + horizontal overflow, since scoped / CSS-module styles can't reach html/body) — but check first, don't blindly add one: Tailwind's preflight (profile.styling.system 'tailwind') and any existing reset/normalize already do this, so only add a minimal global reset when the project is non-Tailwind and has none — and make it complete: zero the body margin AND the default block-element margins (h1–h6, p, ul, figure, …) + box-sizing. A body-only reset still leaves default <p>/heading margins that inflate every stacked text block (e.g. a footer's contact rows get extra spacing and the whole footer grows too tall).`;
};

export const figmaToCodePrompt: {
  definition: Prompt;
  argsSchema: { nodeId: z.ZodOptional<z.ZodString> };
  build: (args: Record<string, string> | undefined) => GetPromptResult;
} = {
  definition: {
    name: FIGMA_TO_CODE_PROMPT_NAME,
    description:
      'Generate framework-aware code from a Figma selection that reuses the project’s existing ' +
      'components and design tokens instead of regenerating them (the cross-client form of the ' +
      'design-to-code workflow: get_design_context + component_map + token_map).',
    arguments: [
      {
        name: 'nodeId',
        description: 'Figma node id to generate from; omit to use the current selection',
        required: false,
      },
    ],
  },
  // McpServer.registerPrompt builds the advertised `arguments` list from this shape; prompt args are
  // always strings, so an optional string mirrors the `nodeId` argument above.
  argsSchema: {
    nodeId: z
      .string()
      .optional()
      .describe('Figma node id to generate from; omit to use the current selection'),
  },
  build: (args): GetPromptResult => ({
    description: 'Figma → code via the three grounded tools (reuse components, reference tokens)',
    messages: [
      {
        role: 'user',
        content: { type: 'text', text: promptText(args?.nodeId) },
      },
    ],
  }),
};
